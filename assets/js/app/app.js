define([
    "jquery"
    , "underscore"
    , "backbone"
    , "router"
    , "template"
], function ($, _, Backbone, Router, Template) {
    var initialize = function () {

    };

    return {
        initialize: initialize
    };
});